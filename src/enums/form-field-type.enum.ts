/**
 * Form Field Type Enum
 * HTML input field types
 */
export enum FormFieldType {
  TEXT = 'text',
  EMAIL = 'email',
  TEL = 'tel',
  TEXTAREA = 'textarea',
  SELECT = 'select',
  NUMBER = 'number',
}
